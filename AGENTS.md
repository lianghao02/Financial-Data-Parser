# 08_Financial-Data-Parser Agent 開發規範

本專案遵循目前有效之全域開發憲法；本檔僅定義專案專屬規則與例外。

---

## 1. 技術棧與前端架構邊界
- **主力架構**：純靜態 Web (HTML5 / Vanilla JavaScript / CSS)。
- **架構純粹性標準**：
  - 本工具核心為零依賴、免建置、瀏覽器即開即用之金融鑑識轉檔工具。
  - **嚴禁引入 React、Vue 等前端框架**，維持標準原生 DOM 操作與極致輕巧。
  - 如需打包獨立離線桌面應用程式，僅允許採用 Tauri (Rust + WebView2)，嚴禁使用 Electron。
- **100% 離線機敏資安保證**：
  - 本工具專用於銀行交易明細、司法偵查與金融帳目解析。所有資料解析、轉檔與 Excel 匯出必須 **100% 於本機瀏覽器記憶體中執行**。
  - 核心相依套件（SheetJS、JSZip）全數由本機內附，**嚴禁發起外部網路請求或載入未授權外部 CDN**。

---

## 2. 業務領域與金融鑑識資料信任核心邊界
- **金融欄位精確度與防失真防線**：
  - **長數字與前導零保護**：身分證字號、銀行帳號、信用卡號、分行代碼開頭的 `0` 必須以純文字格式（String cell）輸出，**嚴禁被 Excel 自動轉為浮點數或科學記號**。
  - **編碼自適應相容**：必須支援公務與銀行常見之 `Big5`、`UTF-8`、`UTF-16 LE/BE` 自動辨識，杜絕中文亂碼。
  - **貨幣符號清洗與金額精確度**：貨幣符號（`$`, `,`, `+`）清理時嚴格保護小數點與負號，不可造成金額數值偏差。
- **資料信任與筆數一致性檢核 (Audit & Data Integrity)**：
  - 轉檔完成後，UI 介面必須透明回報審核數據：
    - 輸入檔案總數、解壓後 CSV 檔數。
    - **來源讀取筆數 vs 匯出寫入總筆數**（必須明確顯示是否 100% 一致）。
    - 若有欄位缺失、格式異常或跳過項目，必須具體提示檔名與列號。

---

## 3. UI/UX 狀態語意
- **狀態與進度透明反饋**：
  - 批次解壓 ZIP 與大容量 CSV 轉檔必須提供清楚的處理進度回饋（當前檔案、處理中分頁、完成下載反饋）。
  - 匯出模式清晰劃分：個別轉檔（保留原檔名）、智慧歸戶（總覽+明細分頁）、資料整合（合併單一大表）。

---

## 4. 核心驗證方式
- 修改 JavaScript 邏輯或腳本後，必須執行語法檢查與品質驗證：
  ```powershell
  node --check js/app.js
  powershell -ExecutionPolicy Bypass -File scripts/qa.ps1
  ```
