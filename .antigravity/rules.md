### 專案細則：金融資料 CSV 轉 Excel 工具
- 強型別保護：帳號/身分證/銀行代碼強制使用 SheetJS `t: 's'` 寫入字串，保留前導零，嚴禁科學記號。
- 金額清洗：嚴格過濾 `$`, `+`, `,`, `=`，並將 `.00` 等後綴清除轉為純整數。
- 檔案遞迴：若 ZIP 內含 ZIP 必須遞迴解壓縮至最底層。
- 堆疊合併 (Vertical Stack)：多檔合併至單一 Sheet 時，僅保留首檔標題列，後續檔案僅提取資料。
- 體驗與防禦：大於 50MB 檔案需顯示全螢幕 Loading；解析失敗須明確報出「錯誤行數」。